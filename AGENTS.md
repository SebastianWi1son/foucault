---
class: fact
generated: false
---
# AGENTS.md — foucault 代理工作规则

> **类：B 事实（规则）**。本文**只放"每次都要遵守"的约束**，压在一屏内。
> 按需流程（批次验收 / 文档对账 / 尺子审计）见 `docs/README.md` §7。
> 文档体系见 [`docs/README.md`](docs/README.md)。压缩前的完整原文见 `trash/AGENTS.md.original`（内容未删）。

## 1. 项目一句话

foucault = 通用可移植**姿态解算核心**（C++17；core 零依赖，禁 STL / 异常 / RTTI / 动态内存）。
**当前实现：Mahony + 3D + 6 轴 acc/gyro + 外部航向注入。** 其余为路线图 → [`docs/STATUS.md`](docs/STATUS.md)。

## 2. 铁律（不可违反）

1. **源码默认只读** —— AI 不得修改 `core/`、`host/`，除非用户明确指示。
   默认允许：`tests/`、`examples/`、`docs/`、新建文件。
2. **用户敲"要懂"的部分** —— AI 给施工单 / 讲解 / 复查；`learning` 区由用户亲手敲。
   `shipping` 区由 AI 写，用户**只审契约**。
3. **命名一致性** —— 用户敲的名字与文档不符时，按命名框架判断：合理则接受并同步文档，不合理则提醒。**不得沉默接受。**
4. **可回退** —— 覆盖 / 删除 / 移动前必须可回退（已提交 / `/tmp` 备份）。三者皆无 = 禁止执行。
5. **不静默吞错** —— 脏数据（NaN/Inf/越界）必须传播或可检出，不得静默洗成合法值。
6. **文档与代码不一致 = 缺陷** —— 发现即记录（进 `trash/` 或 `docs/待办.md`），不许只放在嘴上。

## 3. 命名

- `snake_case`；**成员统一尾下划线 `_`**（struct / class 不作区分）；**参数与局部变量不带 `_`**；bool 成员 = `is_` 前缀 + 尾下划线。
- 禁止 camelCase；领域术语优先（quaternion / residual / innovation / observability）。
- 名字不绑定单位与实现细节（单位进注释与文档）。

## 4. 工程

- C++17；core 仅标准库头文件；host 允许 STL；MCU 侧 `-fno-exceptions -fno-rtti`。
- 告警全开 `-Wall -Wextra -Werror`；改完必过三档编译（默认 / 严格 / ASan+UBSan）。
- 声明与定义分离（数学内核 header-only 例外）；常量与参数聚合进 `Config`。
- 接口层做输入校验（NaN / 模长 / 时间倒退），返回语义明确。

## 5. 测试（AI 写，人签字）

- 测**行为不变式**，不测实现数值；浮点必须用容差，禁止 `==`。
- 每条测试必须有**判别力**：把被测行为改坏 → 必须 FAIL（恒真断言 = 没写）。
- **尺子审计是强制环节**：期望值从哪来 / 输入或期望值是否由被测对象生成 / 改坏一行会不会红。
- 改完必回归：编译 + 全量 `ctest` + 回放金标。

## 6. 沟通

- 全程中文；**先讲原理**（比喻 / 对照表）再动手；**决策点前置**（列清单 + 推荐 + 理由）。
- 面向用户的结论**统一放在回答末尾**，不与过程叙述穿插。
- 外部意见（评审 / 网文 / 其他 agent）**先验证再采纳**，逐条给结论，不盲信也不护短。
- 用户说"等等 / 停下 / 不需要"→ 立即停止。

## 7. 文档

- 三类：**A 日志（只增不改）/ B 事实（唯一来源）/ C 状态（不许手写）**。见 [`docs/README.md`](docs/README.md)。
- **新建文档前必须回答**："它的变更频率和现有哪个文件不同？" 答不出 → 追加，不新建。
- **文档里禁止出现行号**，只写符号名 + 函数名。

## 8. git

- **commit 由用户主导** —— AI 默认不 commit；消息用用户看得懂的中文（改了什么、为什么）。
- **push 前先在本地跑一遍 CI**：`scripts/ci_local.py`。
  它解析 `.github/workflows/ci.yml` 并把每个 job 逐条真跑 —— CI 在 GitHub 上，
  拿日志要等；本地几秒就有完整输出。
- **改了任何文档，顺手跑 `scripts/check_docs.py`**（它也是 CI 的一个 job）。
- 任何 git 操作前先 `git status`；工作区必须干净。
- 文件移动用 `git mv`（保留历史追踪）。
- 流程：dev 分支开发 → 阶段完成回 main 快进合并。

## 9. 错误账本

> **这不是章程，是账本。每一行都对应一次 AI 真实犯过的错。**
> 加行的标准：**这个错如果不写下来，下次还会犯。** 只增不改。

| 日期 | 犯的错 | 加的规则 |
|---|---|---|
| 2026-09-13 | 用被测代码自己的 `rotate` 生成测试输入（循环尺子） | 测试的输入 / 期望值**不得**由被测对象生成 |
| 2026-09-13 | 文档引用 `mahony.cpp:NN` 行号，重构后全部错位 | 文档**禁止行号**，只写符号名 |
| 2026-09-13 | `AGENTS.md` 同时写着"尚未 git init"和"已 git init" | 同一条事实只允许存在一处 |
| 2026-09-13 | 声称"测试写得很全"，但 `Dimension` 测试是恒真断言（删掉目标也不 FAIL） | 每条测试必须验证**判别力**（改坏 → 必须红） |
| 2026-09-14 | 定了"文档禁止行号"，但**没有任何东西在判** —— 存量 56 处一直没人管，规则等于没写 | 能写成脚本判的规则，**必须真有一个脚本**；见 `scripts/check_docs.py` |
|  |  |  |
