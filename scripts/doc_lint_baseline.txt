# 文档门禁 · 存量违规清单（债务清单）
#
# 格式： <规则>  <相对路径>  <允许条数>
#   · 超出条数 → 门禁变红（新引入的违规一律拦下）
#   · 少于条数 → 脚本提醒你把这行改小
#   · 条目消失 → 脚本提醒你删掉这行
#
# 用法：修完一条就把数字改小。这份清单应当【只减不增】。
#       它是明示的、可数的债务，不是规则豁免。
#
# 规则代号看 `scripts/check_docs.py --why`。

# ── R3 行号引用（§6④ 明令禁止）──────────────────────────────────────
# 登记于 2026-09-14。这两份文档通篇拿 `文件:行号` 当锚点，而代码一改就错位
# —— 已经错过一次（trash/README.md 第 4 条：真正门禁在 42 行，文档还写 25）。
# 修法：全文替换为符号名（如 `mahony.cpp` 的 `is_meas_` 门禁）。
# 注意 DESIGN.md 与 待办.md 有大量重复描述，改的时候优先留 DESIGN.md 一处。
R3  docs/DESIGN.md                 4
R3  docs/待办.md                    52

# ── R5 引用的文件必须存在 ───────────────────────────────────────────
# 「把计划当已存在」—— docs/待办.md 的 P2-3 条目早已记着这件事：
#   DESIGN §4.3 的结构树里 core/model/、core/output.h、platforms/、
#   host/allan|calib|plot 均不存在；且 .h / .hpp 扩展名混用（实际全 .hpp）。
# 修法：结构树标注「规划中」，或改成实际布局。
R5  docs/DESIGN.md                20   # §4.3 结构树，计划中的 20 个 .h
R5  docs/待办.md                    1   # P2-3 条目本身在引述那些不存在的路径

# ── R5 已知误报 / 计划中的文件 ──────────────────────────────────────
# 判据：这行是在说「仓库里有这个文件」，还是在引用外部库 / 记录计划？
R5  docs/DEV.md                     1   # 参考库 CtrBoard QuaternionEKF.c，非本仓文件
R5  docs/STATUS.md                  1   # 计划中的生成脚本 scripts/gen_status.sh，尚未创建
